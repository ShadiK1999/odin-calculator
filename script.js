function add(a, b){
    added = parseInt(a + b);
    return added;
    // let fullSum = numbers.reduce((partialSum, a) => partialSum + a, 0);
    // return fullSum;
}

function subtract(a, b){
    return a - b;
}

function multiply(a, b){
    mult = parseInt(a * b);
    return mult;
    // let fullMult = numbers.reduce((partialMult, a) => partialMult * a, 1);
    // return fullMult;
}

function divide(a, b){
    divided = parseInt(a / b);
    return divided;
}

function operate(){

}